#!/bin/bash

SOURCE="${BASH_SOURCE[0]}"
while [ -h "$SOURCE" ]; do # resolve $SOURCE until the file is no longer a symlink
  DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"
  SOURCE="$(readlink "$SOURCE")"
  [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE" # if $SOURCE was a relative symlink, we need to resolve it relative to the path where the symlink file was located
done
root_dir="$( cd -P "$( dirname "$SOURCE" )"/.. >/dev/null 2>&1 && pwd )"
package_dir="$root_dir/resources/app.asar.unpacked"

cd $root_dir/bin

# 数据目录处理
if [ -z "$XDG_CONFIG_HOME" ];then
  DATA_DIR=$HOME/.config
else
  DATA_DIR=$XDG_CONFIG_HOME
fi


# 获取App名称
APP_NAME=$( cat "$package_dir/package.json" | grep -Eo 'name":".*","main' )
APP_NAME=${APP_NAME//name\":\"/}
APP_NAME=${APP_NAME//\",\"main/}

export WECHAT_DEVTOOLS_DIR="$root_dir/electron"
export APPDATA="$DATA_DIR/$APP_NAME"
export USERPROFILE="$APPDATA"
export PATH="$root_dir/bin:$root_dir/electron:$PATH"
mkdir -p "$APPDATA/Default"

"$root_dir/bin/node" "$package_dir/js/common/cli/index.js" "$@"
if [ ! -z "$APPIMAGE" ];then
  # AppImage在程序退出后会删除相关程序文件，导致无法启动
  tail -f /etc/issue
fi
