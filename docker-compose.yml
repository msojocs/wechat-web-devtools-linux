version: "3"
services:
  wechat_devtools:
    image: node:16.13.1
    volumes:
      - .:/workspace
    environment:
      - ACTION=${ACTION_MODE:-false}
      - https_proxy=${https_proxy:-}
    entrypoint: /workspace/docker/entrypoint